generator client {
  provider = "prisma-client-js"
}

model VoicemailOrder {
  id            String   @id @default(cuid())
  email         String
  displayName   String
  script        String
  audioUrl      String?
  paymentMethod String
  createdAt     DateTime @default(now())
}
